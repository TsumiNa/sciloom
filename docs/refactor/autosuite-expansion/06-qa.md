# Questions and evidence requests

Use [difference handling](02-execution-rules.md#difference-handling). Evidence
questions below are C, not authorization to replace semantics or skip validation.

| ID | Question / needed input | Affected stage | Current action |
| --- | --- | --- | --- |
| Q1 | What does the native accumulator do with reset=0/1 in entry, shared child, loop and restart? | R1.2 | Generate minimal probes; keep native status pending. |
| Q2 | Can the screenshot APP or equivalent valid profile be supplied with product version and settings? | R2/R5/R6 | Do not infer September 17 profile compatibility. |
| Q3 | Do existing failure probes terminate child/loop/caller paths? | R2.2 | Retain guard-dependent rejections. |
| Q4 | What bytes and native status do repeated CSV reads/appends produce? | R2.3/R2.4 | Reuse existing probes; UI enum match alone cannot unlock. |
| Q5 | What native text/yes-no results and downstream markers occur for accept, No, cancel, Stop, timeout? | R3.2 | Core/reference can land; native behavior stays gated. |
| Q6 | Why do 20°C/50°C appear as 293.16/323.16? What happens at 0°C, negative values and nonzero gradients? | R5.3 | Standard core SI; precision matrix in thermal measurements doc 37; conversion unverified. |
| Q7 | Which real fixed thermal component/profile, mode and ranges are supported? | R5.3 | Capture profile/mode/ranges with doc 37; finalize typed constructor only after evidence. |
| Q8 | Which transfer tool, channel, calibration, capacity and rinse route form the first valid minimal export? | R6.3 | Capture the single-pair matrix in native measurements doc 38; no assumed tool/rinse defaults. Exact profile contract and native receipts remain pending. |
| Q9 | Can separate stir/heater actuators share a zone and how are physical IDs established? | R4/R5.3 | Use installed component evidence, not zone-name equality or fake namespaces. |

A deviation requiring globals, changed Var persistence, continuing after a
cancel, new legacy JSON meanings or expanded scope must be escalated as D with
alternatives. These are not silently converted into implementation TODOs.

## Version

Version: none, questions and pending evidence only.
