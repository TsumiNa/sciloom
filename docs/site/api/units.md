# Units

Use the root imports `RotationalSpeed`, `rpm` and `rps`. Values are canonicalized to revolutions per second.

::: sciloom.RotationalSpeed

::: sciloom.rpm

::: sciloom.rps

::: sciloom.units.SpeedUnit

