# Runtime text

Import `text` from `sciloom`. Write these operations inside `@runtime` methods;
use ordinary Python string methods for host-time data.

::: sciloom.text
    options:
      members: false

::: sciloom.text.trim

::: sciloom.text.split_part
